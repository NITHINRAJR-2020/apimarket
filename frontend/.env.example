# Base URL of the deployed backend (FastAPI on Render).
# Used only in production builds; dev server uses the Vite proxy in vite.config.ts instead.
VITE_API_BASE_URL=https://apimarket-mp7s.onrender.com
